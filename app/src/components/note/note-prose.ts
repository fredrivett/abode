/**
 * Prose styling for rendered note markdown.
 *
 * Shared by the note card preview and the inline composer so typed text matches
 * exactly how it renders on the card once saved.
 */
export const NOTE_PROSE_CLASS =
  "prose prose-sm dark:prose-invert prose-p:my-[0.4em] max-w-none prose-headings:font-semibold prose-headings:text-[1.05em] prose-headings:text-foreground text-muted-foreground [&>*:first-child]:mt-0 [&>*:last-child]:mb-0";
