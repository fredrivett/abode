{
  "buildCommand": "bunx prisma generate && bunx prisma migrate deploy && bun run build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 1; else exit 0; fi",
  "functions": {
    "src/app/api/v1/items/route.ts": {
      "maxDuration": 300
    }
  }
}
