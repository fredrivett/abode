# testing
/coverage
supabase-e2e/supabase/config.toml

# misc
.DS_Store
.supabase

# secrets / credentials (never commit)
*.pem
*.key
*.p12
*-service-account*.json
gha-creds-*.json

# env files (can opt-in for committing if needed)
.env*
!.env.example

# ai coding
.claude

# setup-wizard artifacts (leak internal project ids / dashboard links)
*-setup-report.md

/node_modules

# Treck generated data
.treck/
.vercel
